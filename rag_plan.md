# 个人 RAG 实施计划 v3（Docker 直连 Tailscale / FNOS）

说明：
- 本计划聚焦可执行流程与运维策略。
- 敏感信息（账号、密码、Token、内网细节）不入库。
- 默认网络路径改为：容器/服务器经 Tailscale 直连 FNOS，不再依赖个人 PC 中转。

## 1. 目标与原则
1. 架构目标
- RAG 主链路稳定、可扩展、低耦合。
- 监控/检索/推理服务在 Tailscale 内可直接访问。

2. 运维原则
- 小改动（文档、小脚本、配置微调）不触发备份。
- 大改动（新功能上线、数据结构变更、路由/端口策略变更）必须执行备份。

## 2. 目标拓扑（新）
- 推理与服务：3090 服务器（宿主 + Docker 容器）。
- 组网：Tailscale P2P。
- 存储：FNOS（`100.109.127.77`）与可选夸克挂载镜像目录。

默认数据与控制流：
1. 业务访问：客户端 -> 容器 Tailscale IP:业务端口。
2. 监控访问：客户端 -> 容器 Tailscale IP:监控端口。
3. 备份路径：服务器/容器 -> FNOS（直连）。

## 3. RAG 运行分层
1. 运行时层
- 模型别名路由：`rag-main` / `rag-fallback`。
- 单卡策略：LM Studio 使用单 GPU（避免跨卡低带宽拖慢）。
- 配置环境变量化：模型 ID、context、并发、GPU 绑定。

2. 检索层
- Qdrant 为主索引。
- 保留 Hybrid 检索与 rerank 插槽。
- 缓存分层：先 Redis L1 exact cache，后续可扩 L2 semantic cache。

3. 网络与转发层
- 宿主本地服务通过反向隧道暴露给容器。
- 容器侧用 socat 将端口绑定到容器 Tailscale IP。
- OpenAPI 控制面管理映射：新增/修改/停用无需改业务代码。

## 4. 变更与备份策略（强制）
1. 不触发备份的变更
- 文案、README、注释、小范围脚本修复。
- 不影响数据结构和可恢复性的配置微调。

2. 必须备份的变更
- 新功能上线或关键流程重构。
- 端口映射策略/转发规则大变更。
- 索引结构、缓存键策略、运行时配置模型大调整。

3. 备份执行标准
- 优先直传：源端 -> FNOS。
- 可选镜像：FNOS 主目录 -> 夸克挂载目录。
- 验收：主备份哈希一致；镜像至少完成 quick 校验（文件名+大小）。

## 5. 执行清单（按优先级）
1. 统一调用入口
- 调用侧全部使用模型别名，不直绑具体模型名。

2. 固化网络流程
- 默认直连；PC 反向隧道仅作为应急 fallback。

3. 运维自动化
- 使用 OpenAPI 管理端口映射。
- 使用备份 skill 执行里程碑备份。

4. 版本管理
- 计划文件与 skill 必须双仓同步更新。
- 小改只提交 Git；大改提交 Git + 执行备份。

## 6. 验收标准
- 服务可通过容器 Tailscale IP + 原端口访问。
- OpenAPI 控制面可查看状态并应用映射。
- 计划与 skill 文档明确“小改不备份、大改备份”。
