# 个人 RAG 知识库实施计划 v2（NAS 存储 + LM Studio 推理）

服务器 ssh jiangzhiming@3090-6.grifcc.top 密码 xidian@jzm


个人 macbook ssh jiangzhiming@10.198.251.140 密码 011223Jzm

飞牛 ssh admin@192.168.31.34 密码 xidian_fnos（飞牛必须通过连接笔记本之后再跳转到这个 ip 连接，因为笔记本和飞牛都在 tailscale 内而本机服务器不在）

## 0. 先回答你的两个关键问题
1. 是否必须上多 Agent 协同？
- 结论：初期不是必须。
- 建议：先做单 Agent 或无 Agent 的标准 RAG（检索 -> 重排 -> 生成），把准确率和稳定性做上去。
- 何时再上多 Agent：当你出现跨库研究、复杂任务分解、工具调用链明显变长时再上。

2. 是否必须用 LangChain？
- 结论：不是必须，但值得用。
- 原因：你当前最重要的是把数据管道和检索质量做稳，框架不是第一优先。
- 建议：
  - 初版可用 LlamaIndex（更偏 RAG 一体化）快速落地。
  - 若你后续要多 Agent 流程编排，可引入 LangChain/LangGraph 作为控制层。

## 1. 当前基础条件（与你现状匹配）
- 你已通过 `lms link` 连到个人 PC。
- 3090 服务器（6x24GB）可承担推理层。
- 数据放校园网飞牛 NAS，满足隐私与本地化要求。
- LM Studio 提供 OpenAI 兼容端点：`/v1/chat/completions`、`/v1/responses`、`/v1/embeddings`，可直接接入主流 RAG 框架。

## 2. 推荐目标架构（分层）
### 2.1 数据层（校园网 NAS）
- 原始资料库：课程、论文、项目文档、知识笔记。
- 解析缓存：文档解析后的中间文本与结构化片段。
- 向量索引持久化：Qdrant 数据目录（建议在 NAS，方便备份和迁移）。
- 备份策略：快照 + 周期归档。

### 2.2 接入与编排层（个人 PC）
- Ingestion 服务：采集、解析、切片、Embedding、入库。
- Query 服务：检索、重排、引用拼接、答案生成。
- 权限服务：按目录/标签/用户做过滤。
- API 网关：对外提供统一问答入口。

### 2.3 推理层（3090 服务器）
- LM Studio/llmster 提供模型推理与 Embedding。
- 模型分工：
  - 主模型（31B）用于复杂回答与最终生成。
  - 小模型用于 query rewrite、轻量分类、路由等低成本任务。

## 3. 技术栈建议（初版到进阶）
### 3.1 初版（MVP）
- RAG 编排：LlamaIndex
- 向量库：Qdrant
- 模型服务：LM Studio OpenAI 兼容 API
- 重排：本地 reranker（可先关闭，作为开关能力）

### 3.2 进阶（需要多 Agent 时）
- 控制层：LangGraph（LangChain 生态）
- 典型 Agent 角色：
  - Planner：任务拆解
  - Retriever：多策略召回
  - Verifier：证据校验与引用检查
  - Writer：组织最终答案
- 触发条件：
  - 单条查询需要多轮工具调用
  - 需要跨知识域协同
  - 需要显式流程审计与回放

## 4. 4 周详细初步方案（可直接执行）
### 第 1 周：打通最小闭环
目标：可问可答、可追溯来源。

任务：
1. 建立目录规范（NAS）
- `source/{domain}/{year}/{project}`
- `metadata/{doc_id}.json`

2. 建立入库流水线
- 文档解析 -> 清洗 -> 切片 -> 向量化 -> 入 Qdrant
- 先支持 PDF/Markdown/Docx 三类

3. 搭建查询 API
- 查询 -> TopK 检索 -> 拼接上下文 -> 31B 生成
- 回答必须附来源（文件名 + 片段位置）

验收标准：
- 50 条样本问答可跑通
- 平均首包延迟可接受
- 每条答案都带引用

### 第 2 周：提升检索质量
目标：降低“检索不到/检索不准”。

任务：
1. 上 Hybrid 检索
- Dense + 关键词/BM25（或 sparse）融合

2. 加重排器
- 对召回结果做 rerank，提升前 3 条证据相关性

3. 建离线评估集
- 100 条问题，标注标准答案与证据文档

验收标准：
- 命中率、引用命中率显著提升
- 幻觉率下降

### 第 3 周：可运维与可控
目标：从“能用”到“可长期运行”。

任务：
1. 增量索引
- 基于 NAS 文件变更（新增/修改/删除）自动更新索引

2. 权限与过滤
- 检索前按用户/组过滤可见文档

3. 观测体系
- 记录检索命中、重排得分、模型延迟、失败原因

验收标准：
- 文档更新后可在 SLA 内检索到新内容
- 权限隔离无越权

### 第 4 周：按需引入 Agent 协同
目标：只在需要时增加复杂度。

任务：
1. 引入轻量 Agent 路由（可选）
- 简单问答走单路 RAG
- 复杂问题走 Planner -> Retriever -> Verifier -> Writer

2. 做模型路由
- 小模型处理改写、分类、摘要
- 31B 只做最终高质量回答

3. 运行手册
- 故障排查、回滚、索引重建流程

验收标准：
- 复杂问题准确率提升
- 平均成本可控

## 5. 关键参数建议（初版默认）
- 切片长度：600~1000 tokens
- overlap：80~150 tokens
- 检索 TopK：20（重排后取 5）
- 回答上下文：优先 16K~32K，避免无意义超长上下文
- 引用策略：至少 2 条证据才给高置信答案

## 6. 风险与规避
1. 风险：只靠向量检索，关键词命中差。
- 规避：尽快上 Hybrid。

2. 风险：文档解析质量差导致“错召回”。
- 规避：解析抽检 + 失败重试 + OCR 兜底。

3. 风险：一开始就上多 Agent，复杂度过高。
- 规避：先单路 RAG，指标稳定后再启用 Agent。

## 7. 你现在就可以执行的第一批事项（本周）
1. 在 NAS 建三层目录：`source/`、`parsed/`、`index/`。
2. 选定首批语料 200~500 份并完成元数据打标。
3. 在个人 PC 起一个 ingestion 脚本，跑首轮入库。
4. 在服务器确认 31B 与一个小模型可并行服务。
5. 建 50 条高价值问题，作为 v1 验收集。

## 8. 最终决策建议（简版）
- 多 Agent：先不强制，按复杂问题占比决定是否启用。
- LangChain：可用但非必须；你可以先 LlamaIndex 快速落地，再用 LangGraph 做多 Agent 编排升级。
- LM Studio：与你当前架构高度兼容，继续作为模型服务层是正确路线。
